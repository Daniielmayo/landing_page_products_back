const { Schema } = require("mongoose");

const SubCategory = new Schema({
    name: String,
    description: String,
    // category:
});

const SubCategroy = ("SubCategory", SubCategory);
module.exports = SubCategroy;
